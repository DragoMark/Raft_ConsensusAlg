# Implementation of RAFT - Distributed Systems

## Run

- `cd` into `./examples`
- run `run_cluster.sh`

## Troubleshooting

- If any errors with `run_cluster.sh`, run `run_cluster.py` directly after adding the raftos module to the python packages path.